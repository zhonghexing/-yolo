# 钢材缺陷检测系统 - 问题解决记录

## 问题一：应用未加载 best.pt 模型

**现象**：启动 GUI 应用后，状态栏显示的模型为 `yolov8n.pt` 而非 `best.pt`。

**原因**：`inference.py` 中的 `find_best_model()` 函数只在 `runs/train/*/weights/` 目录下查找模型，不会检测项目根目录下的 `best.pt`，导致找不到模型后回退到预训练的 `yolov8n.pt`。

**解决**：修改 `inference.py` 中的 `find_best_model()` 函数，优先检查项目根目录下的 `best.pt`：

```python
def find_best_model(project_root=None):
    if project_root is None:
        root = Path(__file__).parent.absolute()
    else:
        root = Path(project_root)

    # 优先使用项目根目录下的 best.pt
    root_best = root / "best.pt"
    if root_best.exists():
        return root_best

    # 其次在 runs/train 目录下查找
    runs_dir = root / "runs" / "train"
    ...
```

---

## 问题二：Web 面板最近检测记录缺少时间和判定

**现象**：Web 监控面板的"最近检测记录"表格中，时间列和判定列显示为空。

**原因**：`web_dashboard.py` 的 `/api/push` 接口直接将 app.py 推送的原始数据通过 WebSocket 转发给前端，但原始数据中不包含 `timestamp`、`has_defect`、`severity` 字段。这些字段在数据库保存时才会计算。

**解决**：在 `api_push` 接口中，推送前补充缺失字段：

```python
# 推送给所有 WebSocket 客户端（补充时间、判定、严重程度）
stats = db.get_stats(24)
has_defect = any(d.get('is_defect', True) for d in detections_data)
overall_severity, _ = db.compute_overall_severity(detections)
enriched = dict(data)
enriched['timestamp'] = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
enriched['has_defect'] = has_defect
enriched['severity'] = overall_severity
socketio.emit('new_detection', {
    'detection': enriched,
    'stats': stats,
})
```

---

## 问题三：Web 面板表格列数不匹配

**现象**：最近检测记录表头为 6 列，但 JavaScript 生成了 7 个单元格，导致列错位。

**原因**：`addRecentRow` 函数中文件名和"查看"按钮分别放在两个 `<td>` 中，但表头没有对应的"操作"列。

**解决**：将文件名和查看按钮合并到同一个单元格：

```javascript
// 修复前：两个独立的 td
`<td>${fileName}</td>
 <td>${imgBtn}</td>`

// 修复后：合并为一个 td
`<td>${fileName} ${imgBtn}</td>`
```

---

## 问题四：Web 面板告警数据显示 undefined 和 NaN

**现象**：最近告警中显示 `undefined 置信度 NaN%`。

**原因**：`addAlert` 函数直接读取 `d.class_name_cn` 和 `d.confidence`，但推送的数据结构中这些字段在 `detections` 数组内部，顶层对象没有这些属性。

**解决**：遍历 `detections` 数组，为每个缺陷生成独立告警：

```javascript
function addAlert(d) {
    if (!d.has_defect) return;
    const dets = d.detections || [];
    const defects = dets.filter(det => det.is_defect);
    defects.forEach(det => {
        // 使用 det.class_name_cn 和 det.confidence 而非 d.class_name_cn
        ...
    });
}
```

> 注：最终用户选择直接移除"最近告警"模块。

---

## 问题五：浏览器缓存导致代码不更新

**现象**：修改了 `web_dashboard.py` 中的 JavaScript 代码后，浏览器仍使用旧版本。

**原因**：浏览器缓存了 HTML 页面，未重新加载最新代码。

**解决**：在 Flask 响应中添加禁用缓存的 HTTP header：

```python
@app.route('/')
def index():
    from flask import make_response
    resp = make_response(render_template_string(HTML_TEMPLATE))
    resp.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    resp.headers['Pragma'] = 'no-cache'
    resp.headers['Expires'] = '0'
    return resp
```

同时提醒用户：修改前端代码后可使用 `Ctrl + F5` 强制刷新浏览器。
